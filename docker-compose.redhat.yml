# NOTICE
# [`rake docker:docs`] This file is generated from a template
#   @see ./templates/docker-compose.redhat.yml.erb for contributing

# Docker compose file to test locally builds for redhat
x-default: &default
  depends_on:
    - db
    - redis

x-default-environment: &default-environment
  RAILS_ENV: production
  NODE_ENV: production
  DECIDIM_FORCE_SSL: false
  SECRET_KEY_BASE: insecure_key_base
  DATABASE_ADAPTER: postgresql
  DECIDIM_SPAM_DETECTION_BACKEND_RESOURCE: memory
  DECIDIM_SPAM_DETECTION_BACKEND_USER: memory

x-default-build: &default-build
  context: ./docker
  dockerfile: ./redhat/Dockerfile

services:
  redhat-decidim-development:
    <<: *default
    build:
      <<: *default-build
      args:
        OS_NAME: "ubi9"
        OS_VERSION: "9.8"
        OS_MAJOR_VERSION: "9"
        DECIDIM_VERSION: "0.33.0.dev"
        DECIDIM_BRANCH: "develop"
        NODE_MAJOR_VERSION: "24"
        BUNDLER_VERSION: "2.4.20"
        RUBY_VERSION: "3.4.7"
        VCS_REF: "2bb23f999857db3a87d769b30d4c9507da195064"
        VCS_REF_SHORT: "2bb23f99"
        VCS_REF_DATE: "2026-09-24"
        VERSION: "0.33.0.dev"
        BUILD_DATE: "2026-09-26T01:10:57+00:00"
    ports:
      - "3001:3000"
    environment:
      <<: *default-environment
      REDIS_URL: redis://redis:6379/0
      DATABASE_URL: postgres://decidim:insecure_password@db:5432/decidim_development

  redhat-decidim-last-stable:
    <<: *default
    build:
      <<: *default-build
      args:
        OS_NAME: "ubi9"
        OS_VERSION: "9.8"
        OS_MAJOR_VERSION: "9"
        DECIDIM_VERSION: "0.32.1"
        DECIDIM_BRANCH: "release/0.32-stable"
        NODE_MAJOR_VERSION: "22"
        BUNDLER_VERSION: "2.4.20"
        RUBY_VERSION: "3.4.7"
        VCS_REF: "d94dd1e7e62214e2e9a91fe0f0a04df0cfaf9dba"
        VCS_REF_SHORT: "d94dd1e7"
        VCS_REF_DATE: "2026-09-23"
        VERSION: "0.32.1"
        BUILD_DATE: "2026-09-26T01:10:58+00:00"
    ports:
      - "3002:3000"
    environment:
      <<: *default-environment
      REDIS_URL: redis://redis:6379/1
      DATABASE_URL: postgres://decidim:insecure_password@db:5432/decidim_last

  redhat-decidim-prev-stable:
    <<: *default
    build:
      <<: *default-build
      args:
        OS_NAME: "ubi9"
        OS_VERSION: "9.8"
        OS_MAJOR_VERSION: "9"
        DECIDIM_VERSION: "0.31.7"
        DECIDIM_BRANCH: "release/0.31-stable"
        NODE_MAJOR_VERSION: "22"
        BUNDLER_VERSION: "2.4.20"
        RUBY_VERSION: "3.3.4"
        VCS_REF: "4b374fe34826c90a5c2be98bd6846b71d720a382"
        VCS_REF_SHORT: "4b374fe3"
        VCS_REF_DATE: "2026-09-24"
        VERSION: "0.31.7"
        BUILD_DATE: "2026-09-26T01:10:59+00:00"
    ports:
      - "3003:3000"
    environment:
      <<: *default-environment
      REDIS_URL: redis://redis:6379/2
      DATABASE_URL: postgres://decidim:insecure_password@db:5432/decidim_prev

  redhat-decidim-legacy-stable:
    <<: *default
    build:
      <<: *default-build
      args:
        OS_NAME: "ubi9"
        OS_VERSION: "9.8"
        OS_MAJOR_VERSION: "9"
        DECIDIM_VERSION: "0.30.9"
        DECIDIM_BRANCH: "release/0.30-stable"
        NODE_MAJOR_VERSION: "18"
        BUNDLER_VERSION: "2.4.20"
        RUBY_VERSION: "3.3.4"
        VCS_REF: "07c31c410fbf29314eae0cde2eb17d0725bdd7dc"
        VCS_REF_SHORT: "07c31c41"
        VCS_REF_DATE: "2026-07-01"
        VERSION: "0.30.9"
        BUILD_DATE: "2026-09-26T01:11:00+00:00"
    ports:
      - "3004:3000"
    environment:
      <<: *default-environment
      REDIS_URL: redis://redis:6379/3
      DATABASE_URL: postgres://decidim:insecure_password@db:5432/decidim_legacy

  redhat-decidim-old-stable:
    <<: *default
    build:
      <<: *default-build
      args:
        OS_NAME: "ubi9"
        OS_VERSION: "9.8"
        OS_MAJOR_VERSION: "9"
        DECIDIM_VERSION: "0.29.7"
        DECIDIM_BRANCH: "release/0.29-stable"
        NODE_MAJOR_VERSION: "18"
        BUNDLER_VERSION: "2.4.6"
        RUBY_VERSION: "3.2.2"
        VCS_REF: "c8535dac229b0d46138570694d28dd7ff37baf8c"
        VCS_REF_SHORT: "c8535dac"
        VCS_REF_DATE: "2025-12-09"
        VERSION: "0.29.7"
        BUILD_DATE: "2026-09-26T01:11:01+00:00"
    ports:
      - "3005:3000"
    environment:
      <<: *default-environment
      REDIS_URL: redis://redis:6379/4
      DATABASE_URL: postgres://decidim:insecure_password@db:5432/decidim_old

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: insecure_password
      POSTGRES_USER: decidim
      POSTGRES_MULTIPLE_DATABASES: decidim_development,decidim_last,decidim_prev,decidim_legacy,decidim_old
    ports:
      - "5432:5432"
    volumes:
      - ./contrib/db:/docker-entrypoint-initdb.d
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "psql", "-U", "decidim", "-c", "SELECT 1 FROM pg_database WHERE datname = 'decidim_prev';"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
